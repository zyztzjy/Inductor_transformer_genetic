# Inductor_transformer_genetic
# Python package dependencies for inductor optimization project
# These packages can be installed via pip:
#   pip install -r requirements.txt
#
# Note: ADS_inductor_layout.py requires the 'keysight.ads.de' module,
# which is part of Keysight ADS Python API and must be installed separately
# from the ADS software distribution. It is not available on PyPI.

pandas==2.2.0
numpy==1.26.3
torch==2.2.0
scikit-learn==1.4.0
matplotlib==3.8.2
